<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.learnkannada.com.learnkannadakannadakali">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".Homepage"/>
        <activity android:name=".SplashScreen_Activity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChooseCourseActivity" />
        <activity android:name=".CourseHomePageActivity" />
        <activity android:name=".DayActivity" />
        <activity android:name=".StagesActivity" />
        <activity android:name=".ExampleActivity"></activity>
    </application>

</manifest>